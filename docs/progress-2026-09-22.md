# 项目接续记录 · 2026-09-22

## 基线与本次完成

- Git 从 `e8cf4e7` 快进到 `5266081`（远端 main，4 个提交），更新前工作区干净。
- 已阅读 AGENTS.md 及架构、Python 契约、Dify 工作流和开发清单；执行顺序仍以 AGENTS.md §3 为准。
- 更新后 35 项测试通过。本次继续加固 M1 的 Python 适配器，补充请求截止、禁止重定向、任务／尝试／产品绑定、受控文件引用和响应字段白名单。
- 第一轮修改后 39 项测试通过；用户确认香港资料明天提供，继续完成下述本地验收。
- 业务服务增加状态跳转限制和适配器 patch 白名单，禁止覆盖身份／快照。真实文件不能由 adapter 自行批准交付；状态／候选元数据／文件与对应审计原子保存，失败时一起回滚，真实任务审计不再错误标注为模拟执行服务。
- 增加确认到期与篡改、不同幂等键重复确认、非法结果、故障回滚和磁盘重启测试；HTTP 闭环覆盖经纪／租户隔离、未完成下载拒绝、复核导出、编辑失效与旧方案导出阻止。
- **Node v24.21.0：46 项测试全部通过**。通过 npm 缓存取得 Node 24，未更改本机默认 Node（仍为 v22.23.1）。
- 在 Git 忽略的 `tmp/python-runtime` 安装隔离的 `reportlab==4.4.9` 环境，新增 `npm run test:smoke`。实际 HTTP → Python 模拟 PDF（4010 bytes）→ hash 核对 → 复核导出 → 重启后下载和幂等重放全部通过。临时数据库已自动清理。
- 继续完成离线参数抽取加固：多个金额／性别／吸烟状态冲突不再取第一个；无效精度、科学记数法、错误千分位与越界字段留空；年龄、币种、缴费年期按当前演示 schema 校验；同值重复输入允许合并。所有候选依据均为输入原文片段。
- 新增 70 条显式参数语料（35 条有效、35 条冲突／边界），加上称谓不推断性别、收益不转成保费、助手不自动提交的集成用例。这是有限规则的离线回归，**不是**真实 Dify／模型准确率验收，也不能保证识别任意自然语言歧义。
- `/api/extract` 现在要求 `schemaVersion` 与当前字段定义一致，缺失／旧版返回 `409 SCHEMA_CHANGED`；结果含 `productId`、`schemaVersion`、`requiresConfirmation: true`。演示登录只接受明确的角色名；PDF 下载重新核对存储内容与生成时 hash，不一致返回 `FILE_INTEGRITY`。
- **最新验收：Node v24.21.0 下 120 项测试全部通过，实际 Python PDF 完整流程再次通过，`git diff --check` 通过。**
- 按 B05 补上 SQLite 持久任务租约、心跳续租和结果提交时的 token／快照校验。真实执行尝试在调用前落库，过期、失去租约及结果保存失败均不会自动重提。恢复跳过其他 worker 的有效租约；adapter 收到独立快照，不能通过原地修改污染业务任务。
- 增加 9 项租约／事务验证，其中包含两个独立 Node 子进程共用数据库、执行中续租、取消信号、模拟任务接管、迟到结果阻止、真实尝试持久化和存储故障。
- **当前最新验收：Node v24.21.0 下 129 项测试全部通过，实际 Python PDF 完整流程通过，`git diff --check` 通过。** 多进程验证只覆盖本机 SQLite；远端 Worker 协议与门户账号并发限制仍待真实集成。
- 本次修改保留在工作区，未 commit、push 或创建 PR。

## 2026-09-23 接续：C04 本地运营控制

- 实现租户内产品暂停／恢复接口与运营表单，必填原因和版本校验；经纪仅查看。旧确认页不能绕过暂停，已接受任务的幂等重放仍保留原语义。
- 排队任务转人工，领取后发生暂停时再次阻止执行；已在途任务继续核对结果。恢复不自动重新提交，模拟任务由运营单独处理。
- `PORTAL_CHANGED` 可自动暂停已登记产品；运营处理说明与任务状态原子提交。真实目录、门户识别及现场验收仍未完成。
- 新增 8 项测试；浏览器在隔离演示库中完成运营身份切换、暂停、原因展示与恢复点验。
- **当前最新验收：Node v24.21.0 下 137 项测试通过，实际 Python PDF 完整流程通过。** 操作与接口说明见 [产品暂停与人工处理](product-operations.md)。

## 当前交付状态

### 2026-09-23 接续：完整产品确认快照

- 草稿与任务保存完整 `productSnapshot` 及 hash；确认参数 hash 同时绑定产品版本、schema 版本和规则快照。创建任务前重新验证快照完整性及当前目录一致性，官方版本、字段约束、保司或执行方式漂移均需重新确认。
- 旧版缺少完整产品快照的未提交草稿返回 `SCHEMA_CHANGED`，需重新创建；已接受任务的原幂等键仍返回原任务。历史任务缺少新字段时保留原有演示读取兼容路径，不补造真实资料。
- 确认页、任务参数区和讲解包使用保存的字段定义；表单金额校验改为读取当前 schema 上下限。
- 新增 5 项版本／完整性／历史读取／范围测试。Node v24.21.0 下 **149 项测试通过**；完整快照改动后的实际 Python PDF 流程已通过，`git diff --check` 通过。

### 2026-09-23 接续：执行账号隔离

- 增加持久 `execution_resources`，同一服务端 `credentialRef` 的不同任务串行执行，跨租户也不会重复占用；缺少引用时共用保守执行槽。
- 账号占用在结果不确定、重启或租约失效后保留；运营明确核实门户结束后关闭任务，账号释放与审计一起提交。契约明确的参数／产品拒绝可释放，未知错误不能释放。
- 新增 7 项测试覆盖同账号／不同账号、跨租户、缺配置、重启、越权、审计回滚和有效租约阻止释放。
- 最新验证：Node v24.21.0 下 **144 项测试通过**，实际 Python PDF 完整流程通过，`git diff --check` 通过。
- 香港真实账号引用映射、执行前拒绝语义、远端锁及门户会话隔离仍待资料和现场验证。

| 范围 | 状态 | 下一步 |
| --- | --- | --- |
| 本地演示闭环、M1a adapter | 已实现，本次增加边界测试 | 保持回归通过 |
| M1b 香港 Python 真实链路 | 等待外部输入，未完成 | 读取已有脚本并验证一社一产品 |
| M2a-1 / M2a-2 | 离线实现已完成 | 真实部署前保留未配置状态 |
| M2b Dify / DeepSeek | 等待环境，未开始真实联调 | 获取获准实例、模型部署方式和受控密钥配置 |
| M3 知识库 | 未开始 | 获得审核通过的官方资料、权限及版本元数据 |
| APP IM、企业身份、生产部署及试点 | 未完成 | 明确 APP 工程、身份协议和部署环境，完成真实验收 |

## 下一项：M1b

需要香港侧提供可访问的脚本目录、仓库或已授权连接方式，以及入口与运行命令、依赖、首家保司／产品官方版本、字段约束、PDF 获取方式、登录／MFA 流程、3–5 组脱敏官方输入与 PDF 样本。凭据由受控配置提供 `credentialRef`，不把密码或真实客户资料放入聊天或代码库。

取得资料后，先核对并复用现有入口和任务机制，再确定同步调用或异步受理方式，实现鉴权、任务尝试持久化、真实字段核对及受控文件下载；异常和无法核验的结果转人工。完成官方样本、跨任务错配与中断恢复验收后才能标记 M1b 完成。

当前没有足够外部资料完成真实保司上线；本次离线加固不等于整项目完成。

## 可重复验收

本机默认 Node 22，因此本次使用以下命令；已安装 Node 24+ 的环境可直接执行 `npm test` 和 `npm run test:smoke`。

```sh
npm exec --yes --package=node@24 -- node --test tests/*.test.mjs
HB_PYTHON="$PWD/tmp/python-runtime/bin/python" npm exec --yes --package=node@24 -- node scripts/smoke.mjs
git diff --check
```
